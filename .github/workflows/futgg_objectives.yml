name: FUTGG Raw Objectives Crawler

on:
  schedule:
    # 한국시간 매일 03:00 = UTC 매일 18:00
    - cron: "0 18 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  crawl:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Python packages
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright browser
        run: |
          python -m playwright install chromium

      - name: Run raw crawler
        timeout-minutes: 15
        env:
          PYTHONUNBUFFERED: "1"
        run: |
          mkdir -p outputs
          echo "=== raw crawler start ==="
          date
          python -u futgg_raw_objective_crawler.py --out outputs --headless
          echo "=== raw crawler end ==="
          date
          echo "=== outputs after raw crawler ==="
          ls -la outputs

      - name: Upload result files
        uses: actions/upload-artifact@v4
        with:
          name: futgg-raw-objectives-result
          path: outputs/

      - name: Update latest raw files
        run: |
          mkdir -p latest

          echo "=== outputs files before copy ==="
          ls -la outputs

          cp outputs/objectives_raw_latest.xlsx latest/objectives_raw_latest.xlsx
          cp outputs/objectives_raw_latest.json latest/objectives_raw_latest.json

          echo "=== latest files ==="
          ls -la latest

      - name: Commit latest raw files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git status --short
          git add -A latest
          git status --short

          git commit -m "Update latest raw objectives" || echo "No changes to commit"
          git push
